apiVersion: apiextensions.crossplane.io/v1
kind: Composition
metadata:
  name: {{ include "wireguard-gateway-operator.componentName" (dict "ctx" . "component" "xgateway-network") }}
  labels:
    {{- include "wireguard-gateway-operator.labels" (dict "ctx" . "component" "xgateway-network") | nindent 4 }}
    provider: gcp
spec:
  compositeTypeRef:
    apiVersion: infra.wgnet.dev/v1alpha1
    kind: XGatewayNetwork
  mode: Pipeline
  pipeline:
    - step: render-network
      functionRef:
        name: function-go-templating
      input:
        apiVersion: gotemplating.fn.crossplane.io/v1beta1
        kind: GoTemplate
        source: Inline
        options:
          - missingkey=error
        inline:
          template: |
{{ .Files.Get "crossplane/gcp/network-composition.gotmpl" | nindent 12 }}
    - step: ready
      functionRef:
        name: function-auto-ready
