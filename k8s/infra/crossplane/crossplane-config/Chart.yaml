apiVersion: v2
name: crossplane-config
description: GCP ClusterProviderConfig for the gateway provisioner (requires the provider-installed CRDs)
type: application
version: 0.1.0-dev
appVersion: "0.0.0-dev"
