apiVersion: v2
name: crossplane-providers
description: GCP and Kubernetes providers and pipeline functions for the gateway provisioner, with a post-install gate that blocks until provider CRDs are established
type: application
version: 0.1.0-dev
appVersion: "0.0.0-dev"
